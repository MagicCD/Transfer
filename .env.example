# 文件快传应用程序环境变量配置示例
# 复制此文件为 .env 并根据需要修改
# 所有配置项都有默认值，此文件中的值将覆盖默认值

# =====================================================================
# 应用基础配置
# =====================================================================

# 应用程序密钥，用于会话加密等安全功能
# 必需项，类型：字符串
SECRET_KEY=your-secret-key-here

# 服务器监听端口
# 必需项，类型：整数，范围：1024-65535
SERVER_PORT=5000

# 是否启用调试模式
# 可选项，类型：布尔值，默认值：false（开发环境为true）
# DEBUG=true

# 是否启用安全Cookie（生产环境推荐启用）
# 可选项，类型：布尔值，默认值：false（生产环境为true）
# SECURE_COOKIES=true

# 运行环境设置
# 可选值：development, production, test，默认值：development
FLASK_ENV=development

# =====================================================================
# 文件上传配置
# =====================================================================

# 上传文件存储目录
# 必需项，类型：字符串，如果目录不存在将自动创建
UPLOAD_FOLDER=uploads

# 临时分块文件存储目录
# 必需项，类型：字符串，如果目录不存在将自动创建
TEMP_CHUNKS_DIR=uploads/.temp_chunks

# 最大上传文件大小（字节）
# 必需项，类型：整数，范围：1MB-10GB
# 默认值：5GB = 5368709120 字节
MAX_CONTENT_LENGTH=5368709120

# 文件分块大小（字节）
# 必需项，类型：整数，范围：1MB-100MB
# 默认值：5MB = 5242880 字节
# 注意：必须小于或等于分块上传阈值
CHUNK_SIZE=5242880

# 启用分块上传的文件大小阈值（字节）
# 必需项，类型：整数，范围：1MB-1GB
# 默认值：50MB = 52428800 字节
# 注意：必须小于或等于最大内容长度
CHUNKED_UPLOAD_THRESHOLD=52428800

# =====================================================================
# 缓存和临时文件配置
# =====================================================================

# 文件列表缓存有效期（秒）
# 必需项，类型：整数，范围：1-3600
# 默认值：开发环境 5 秒，生产环境 30 秒
FILES_CACHE_TTL=5

# 临时文件最长保存时间（小时）
# 必需项，类型：整数，范围：1-168
# 默认值：开发环境 2 小时，生产环境 24 小时
TEMP_FILES_MAX_AGE=2

# =====================================================================
# 日志配置
# =====================================================================

# 日志级别
# 必需项，类型：字符串
# 可选值：DEBUG, INFO, WARNING, ERROR, CRITICAL
# 默认值：开发环境 DEBUG，生产环境 INFO
# LOG_LEVEL=DEBUG
